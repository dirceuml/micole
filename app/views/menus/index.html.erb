<h1>Listing menus</h1>

<table>
  <tr>
    <th>Menu</th>
    <th>Transaccion</th>
    <th>Orden</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @menus.each do |menu| %>
  <tr>
    <td><%= menu.menu_id %></td>
    <td><%= menu.transaccion_id %></td>
    <td><%= menu.orden %></td>
    <td><%= link_to 'Show', menu %></td>
    <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
    <td><%= link_to 'Destroy', menu, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu', new_menu_path %>
